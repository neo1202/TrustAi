const QueryTheOracle = ({ data }) => {

    return <div style={{display:'flex'}}>
        <div>{data}</div>
        <div style={{marginLeft:'20px'}}>__________(text area or options)</div>
    </div>
}

export default QueryTheOracle;
